# 토큰 효율성과 통제(Enforcement) 기획서

> **안내**: 본 기획은 Codex의 심층 분석 결과를 바탕으로 작성되었습니다. (Mistral 및 Deepseek 등 다른 로컬 모델에게도 질의를 시도했으나, 현재 백그라운드의 Ollama 서버 초기화 에러(`Failed to start: Unable to init instance`)로 인해 응답을 받지 못했습니다. 따라서 현존 최고 추론 모델인 Codex의 기획안을 중심으로 전개합니다.)

---

## 1. 수학적/구조적 분석: 왜 압도적으로 "토큰 효율적"인가?

새로운 아키텍처(Architect/Editor 분리 및 Diff 패치 방식)는 단순히 프롬프트 몇 줄을 아끼는 것이 아닙니다. **비용(토큰) 발생의 근본적인 기준(함수)을 바꿉니다.**

### A. 비용 모델의 수학적 차이
*   **기존 (전체 파일 주입 방식)**
    *   비용 함수: `입력 비용 ≈ 반복 횟수(T) × 전체 파일 크기의 합(ΣF)`
    *   예: 10,000 토큰짜리 파일 5개를 8번의 루프 동안 넣는다면 `8 × 50,000 = 400,000 토큰`이 소모됩니다. (실제 수정하는 코드는 단 10줄이어도 동일)
*   **신규 (Context Broker + Diff 방식)**
    *   비용 함수: `입력 비용 ≈ 반복 횟수(T) × (필요한 문맥 조각 크기) + 변경 크기`
    *   예: 매 턴 3개 위치에서 250토큰씩만 제공하면 `8 × 750 = 6,000 토큰`.
    *   **결과**: 입력 토큰이 파일 단위에서 "변경 범위 단위"로 축소되어 **수십 배(위 예시 기준 약 66배)의 토큰 절감**이 발생합니다.

### B. 출력(Output) 토큰과 회귀(Regression) 위험의 동시 감소
전체 파일을 다시 쓰는 구조에서는 변경되지 않은 99%의 코드를 다시 생성하느라 막대한 출력 토큰이 낭비됩니다. Diff 블록(`SEARCH/REPLACE`) 방식은 출력 비용을 오직 **변경점의 크기**에 비례하게 만듭니다. 게다가 불필요한 코드를 재생성하지 않으므로, 원래 잘 작동하던 코드를 망가뜨리는 환각(Hallucination) 에러율이 급감합니다.

### C. 어텐션(Attention) 낭비 제거
컨텍스트가 길어지면 LLM은 주의력(Attention)을 분산시킵니다. 관련 없는 유틸리티 함수나 오래된 주석에 시선을 뺏겨 엉뚱한 코드를 짜게 됩니다. Context Broker가 정확한 Snippet만 제공하면 탐색 공간이 줄어들어 추론 품질과 속도가 극도로 상승합니다.

---

## 2. 강제성(Enforcement) 부여 기획: 어떻게 이탈을 막을 것인가?

LLM에게 "제발 전체 코드를 쓰지 마", "무단으로 파일 읽지 마"라고 프롬프트로 **부탁**하는 것은 실패합니다. 스크립트 레벨에서 기계적이고 물리적인 **통제(Enforcement)** 장치를 마련해야 합니다.

### A. 상태 머신(State Machine)에 의한 출력 문법 강제
모델의 출력을 파싱하는 스크립트(하네스)는 엄격한 정규식이나 파서를 통해 텍스트를 검증합니다.
*   **허용된 출력 포맷**:
    1. `CONTEXT_REQUEST` (더 많은 코드 문맥이 필요할 때)
    2. `SEARCH_REPLACE_PATCH` (실제 코드 수정 시)
*   **이탈 시 즉시 거부(Reject)**:
    모델이 파일 전체를 뱉거나 쓸데없는 마크다운 설명을 길게 늘어놓으면, 스크립트는 이를 즉시 폐기하고 다음의 짧은 에러를 모델에게 던집니다.
    > *"Rejected: output_type must be SEARCH_REPLACE_PATCH or CONTEXT_REQUEST. Do not explain. Emit one valid block only."*

### B. Context ID 바인딩 (환각 수정 원천 차단)
로컬 LLM이 "자신이 보지 못한 파일 영역"을 상상해서 패치하는 것을 막는 가장 강력한 기법입니다.
1. **Broker의 제공**: Broker가 코드를 줄 때 고유 ID를 붙여서 줍니다. (예: `CONTEXT id=ctx_17 file=src/example.ts lines=120-165`)
2. **Editor의 참조**: 로컬 모델이 Diff를 작성할 때는 반드시 `*** Context: ctx_17` 처럼 ID를 명시해야 합니다.
3. **Applier의 검증**: 패치를 적용하는 스크립트는 `SEARCH` 블록의 내용이 `ctx_17` 원본 안에 정확히 존재하는지 검증한 뒤에만 덮어씁니다. 없으면 즉시 거부(Reject)합니다.

### C. 물리적 권한의 완전한 박탈 (Privilege Separation)
로컬 모델에게 파일을 마음대로 열어볼 수 있는 파일 시스템 권한(Tool) 자체를 주지 않습니다.
*   **Context Broker (스크립트)**: 유일하게 파일을 읽고 쓸 수 있는 권한을 가집니다.
*   **Editor (로컬 LLM)**: 오직 표준 입력(stdin)으로 받은 Text 조각(Context)만 읽을 수 있고, 다시 텍스트(Diff)만 반환합니다. 물리적 파일 접근 권한이 0%이므로, 무단으로 파일을 읽는 행위 자체가 불가능해집니다.

---

### 요약
이 아키텍처는 **비용 기준을 "파일 크기"에서 "변경 크기"로 전환**하여 압도적인 토큰 효율성을 달성합니다. 또한, 모델의 자율성에 의존하지 않고 **물리적 권한 박탈과 엄격한 출력 포맷 거부(Reject) 루프**를 통해 에이전트의 이탈을 스크립트 레벨에서 완벽히 통제(Enforce)합니다.
