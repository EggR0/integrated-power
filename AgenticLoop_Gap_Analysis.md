# 현재 구조와 목표 구조의 본질적 괴리 및 최소 필수 안전장치 분석

> **참고**: 이 문서는 사용자의 요청에 따라 Codex(`Invoke-CodexDebate.ps1`)가 심층 분석한 결과를 바탕으로 작성되었습니다.

---

## 1. 본질적 괴리(Gap): "누가 룰을 강제(Enforce)하는가?"

현재 저(Gemini)를 오케스트레이터로 사용하여 로컬 LLM과 Codex를 지휘하는 방식은 겉보기에는 `계획 -> 실행 -> 관찰 -> 재시도`라는 에이전틱 루프(Agentic Loop)처럼 보입니다. 하지만 본질적으로 이것은 **"AI의 도움을 받는 수동 오케스트레이션(AI-assisted manual orchestration)"**에 불과합니다.

가장 큰 차이는 **판단의 품질이 아니라, 판단의 위치**에 있습니다.

| 항목 | 현재 구조 (수동 오케스트레이션) | 목표 구조 (자율 파이프라인) |
| :--- | :--- | :--- |
| **통제 주체** | **저(Gemini)의 지능과 컨텍스트 창**에 전적으로 의존. 제가 지치거나 토큰 한계에 다다르면 루프가 무너짐. | **상태 기계(State Machine) 스크립트**가 기계적으로 룰을 강제함. |
| **컨텍스트 선택** | 제가 파일과 로그를 직접 읽고, "적당히" 복사해서 프롬프트에 넣어 다른 모델에게 전달함. | **Context Broker**가 예산 내에서 코드를 자르고 요약하여 배급함. |
| **반복/종료 제어**| 제가 판단하여 멈춤. 판단을 잘못하면 무한 루프. | 예산(Budget), 횟수 제한(Circuit Breaker)이 도달하면 스크립트가 강제 차단. |
| **검증 책임** | 모델이 "완료했습니다"라고 하면 제가 그것을 다시 읽고 판단. | **Validator**가 테스트 통과, 정규식 매칭 등 기계적 통과 여부만 판단. |

즉, 현재의 구조는 제가 똑똑하게 굴면 잘 돌아가지만, 시스템적 강제성(Invariants)이 없기 때문에 언제든 토큰 폭발이나 무한 루프에 빠질 수 있는 **유리 대포**와 같습니다.

---

## 2. 당장 적용해야 할 "최소한의 필수 안전장치" 7가지

완전한 Context Broker를 당장 짜지 않더라도, 현재의 PowerShell 파이프라인과 저의 프롬프팅 워크플로우에 **지금 즉시** 도입해야 할 최소한의 방어막(Guardrails)은 다음과 같습니다.

### ① Run Envelope (실행 예산 봉투)
다른 모델을 호출할 때 막연히 "이거 해줘"라고 던지지 않습니다. 모든 호출(위임)에는 반드시 **예산과 제약(Envelope)**이 붙어야 합니다.
*   `max_turns=3` (최대 3번만 대화)
*   `sandbox=read-only` (파일 무단 변경 금지)
*   `stop_on_repeated_failure=2` (동일 에러 2회 발생 시 즉시 중지)

### ② Context Budget Gate (컨텍스트 명세서)
파일 전문을 때려 넣는 악습을 당장 끊어야 합니다. 프롬프트 최상단에 다음과 같은 `Context Manifest` 표를 작성하여 제한된 문맥만 전달합니다.
*   *오류 로그는 전체가 아닌 `tail 200 lines`만 전달.*
*   *파일은 전체가 아닌 `lines 120-150`만 추출하여 전달.*
*   "컨텍스트에 없는 파일 내용은 절대 상상하지 말고 `CONTEXT_REQUEST`를 호출하라"고 지시.

### ③ Anti-Amnesia Ledger (실패 장부 주입)
데스 루프의 원인은 모델이 같은 실수를 반복하는 것입니다. 호출 프롬프트 하단에 반드시 이전의 실패 기록을 요약해서 꽂아 넣습니다.
> *Attempt Ledger: 1차 시도에서 A 코드를 넣었다가 B 에러가 났음. 절대 A 코드를 다시 쓰지 말 것.*

### ④ Output Schema Gate (출력 타입 강제)
자유로운 마크다운 응답을 허용하면 파싱이 불가능해집니다.
실무를 담당하는 로컬 모델의 출력은 오직 `SEARCH_REPLACE_PATCH`, `CONTEXT_REQUEST`, `BLOCKED_REPORT` 중 하나로만 제한하고, 이외의 변명이나 긴 설명이 섞이면 파이프라인이 가차 없이 `Reject(거부)` 하도록 파서를 엄격하게 만듭니다.

### ⑤ Validator Gate (완료의 재정의)
**"모델이 완료했다고 말하는 것은 완료가 아니다."**
코드 수정이 일어났다면 스크립트가 테스트(문법 체크, 린트 등)를 실행하고, `exit_code == 0` 일 때만 상태를 `Done`으로 전이시킵니다. 사람이나 모델의 정성적 판단을 배제합니다.

### ⑥ Circuit Breaker (물리적 차단기)
*   같은 에러 2회 반복
*   단일 작업에서 모델 3회 호출 초과
*   토큰 한도 초과
위 조건 중 하나라도 달성되면 하위 루프는 즉시 작업을 멈추고 `BLOCKED_REPORT`를 생성한 뒤, 상위 모델인 저(Gemini)에게 진단을 요청(Escalation)해야 합니다. "멈춤"도 정상적인 상태로 인정해야 무한 루프를 막습니다.

### ⑦ Privilege Separation (권한 계층화)
아키텍처 논의나 코드 리뷰를 할 때는 스크립트를 무조건 `Read-Only` 모드로 돌립니다. 실제 코드를 패치할 때만 `Workspace-Write` 권한을 부여합니다. 이렇게 권한을 등급별로 나누어 무단 수정을 원천 차단합니다.
